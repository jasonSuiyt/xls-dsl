import { InputNoSpaceDirective } from './input-no-space.directive';

describe('InputNoSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNoSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
